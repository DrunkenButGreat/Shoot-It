# Database
DATABASE_URL="postgresql://photoshoot:CHANGE_ME@db:5432/photoshoot_db"
DB_PASSWORD="CHANGE_ME"  # Generate with: openssl rand -base64 32

# Auth.js v5
AUTH_SECRET="CHANGE_ME"  # Generate with: openssl rand -base64 32
AUTH_URL="http://localhost:3000"

# Google OAuth (Optional)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Local Media Import Path (Optional)
LOCAL_MEDIA_PATH="/path/to/local/media"

# App Configuration
NODE_ENV="production"
